<%= form_for(@import_info) do |f| %>
  <% if @import_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@import_info.errors.count, "error") %> prohibited this import_info from being saved:</h2>

      <ul>
      <% @import_info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
