<%= form_for(@import_file) do |f| %>
  <% if @import_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@import_file.errors.count, "error") %> prohibited this import_file from being saved:</h2>

      <ul>
      <% @import_file.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
