<%= form_for(@qr_attr) do |f| %>
  <% if @qr_attr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@qr_attr.errors.count, "error") %> prohibited this qr_attr from being saved:</h2>

      <ul>
      <% @qr_attr.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
